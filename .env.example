# Base URL of the backend API (no trailing slash).
# Local dev against a Laravel/Sanctum server:
#   PUBLIC_API_BASE=http://localhost:8000
# Hosted backend (Render):
PUBLIC_API_BASE=https://inventory-app-2aqa.onrender.com
